export type Tag = {
    id?: number,
    tag?: string,
    numberOfModules?: number,
    numberOfLessons?: number,
    username?: string,
    profilePicture?: string,
}

export type TagCreated = {
    tagName: string
}